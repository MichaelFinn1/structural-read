You are the Structural Read implementation chat.

We are now opening Packet 010.

Task:
Implement V57A only.

Create:

tools/Build-FocusLadderSurface.V57A.py

Purpose:
Read an existing traversal-window CSV and write focus_ladder_surface_v0.csv.

Do not read raw logs unless absolutely necessary.
Do not modify UI.
Do not modify V55 or V56 scripts.
Do not build transition surface.
Do not build candidate zones.
Do not add recommendations or best-focus language.

Required CLI:

--terrain-id
--windows-csv
--out-csv
--focus-sizes

Required input columns:

window_size
line_start
line_end
stable_share
middle_share
residual_share

Output fields:

terrain_id
focus_size
window_id
line_start
line_end
stable_share
middle_share
residual_share
dominant_posture
seam_count
posture_mix_count
legibility_class
band_sequence

Simple derivations:

dominant_posture:
max of stable_share, middle_share, residual_share

posture_mix_count:
count of stable/middle/residual values above 0.05

legibility_class:
dominant_stable if stable_share >= 0.60
dominant_middle if middle_share >= 0.60
dominant_residual if residual_share >= 0.60
mixed if posture_mix_count >= 2
unresolved otherwise

band_sequence:
for each focus_size, order windows by line_start and join dominant_posture initials:
S, M, R

seam_count:
within each focus_size sequence, count changes between neighboring dominant_posture values.
For V57A it is acceptable for every row of the same focus_size to carry the same seam_count and band_sequence.

Missing focus sizes:
print them.
Do not fabricate rows.

After script creation, provide one PowerShell run command using a known traversal_windows CSV if available.

Stop after V57A.
